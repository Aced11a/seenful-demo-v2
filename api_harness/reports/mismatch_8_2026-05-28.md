# 8 条 mismatch 为什么没通过

- **数据源**: `api_harness/results/run_20260528_122718.json` · 2026-05-28
- **总览**: 76 case 跑完,**31 条真发请求**(23 ✓ + 8 ✗),45 条没发(4 BLOCKED + 41 deferred)
- **本报告只讲这 8 条 mismatch 为什么没过**,编号对应 dashboard `_DASHBOARD.html` 顶部的 `#N`
- 期望按"产品意图应不应该成集"写,**不改 expected 迎合后端**(CLAUDE 第4条)。下面每条 = 产品意图 ≠ 后端实际的具体 gap

---

## 速读 · 8 条按机制归到 3 类

| 类 | 涉及 | 共同症状 |
|---|---|---|
| **A 后端 L2 主路径阈值偏严**(5 条) | #28 #29 #34 #41 #44 | 多元主题/多场景的 trigger 被判 suppress 或 show_inline_hint,产品意图允许成集 |
| **B 后端 L2.5 维度判定不细**(2 条) | #47 #59 | L2.5 路径要么不识别 people 维度(#47),要么不识别 event 子类型冲突(#59) |
| **C 新码值待映射**(1 条) | #61 | sensitive 在 L2.5 返回新码 `not_applicable`,语义疑似 = no_merge,等后端确认就能 match |

---

## A 类 · L2 阈值偏严(5 条)

### #28 · FP_B2_li_th0_reverse

| 字段 | 内容 |
|---|---|
| 路径 | L2 |
| 产品意图 | `show_mini_album`(网红打卡 6 张应成集) |
| 后端实际 | `suppress` · pattern `truth_table_final_F1` · album=[] |
| trigger 主题(6 张速览) | l60 奶茶 / l61 排队人群 / l62 街道地砖 / l63 橱窗反光 / l64 街景 / l65 地标 |

**为什么 mismatch**:6 张是"上海一日网红打卡"的多场景集合(奶茶店 → 街头 → 橱窗 → 地标),**每张主题不同但同属一次出行**。后端 truth_table_final_F1 = "不命中以上"兜底判 suppress,**主题分散度阈值偏严**就把这种"多元同行程"判没了。产品意图希望成"一日打卡集"。

---

### #29 · FP_B3_zhang_event_activity_walk_drill

| 字段 | 内容 |
|---|---|
| 路径 | L2 |
| 产品意图 | `show_mini_album` |
| 后端实际 | `show_inline_hint`(轻提示)· pattern `truth_table_final_D3` · album=[] |
| trigger 主题(5 张) | fz19 早餐 / fz20 办公会议 / fz21 公园独走 / fz22 茶室聊天 / fz23 其他 |

**为什么 mismatch**:5 张分布在"早餐 → 会议 → 散步 → 茶室"四种不同活动,**event_hint 维度其实有 walk + meal + work 多种**。后端 D3 判"中等多主题"给轻提示,但产品意图把它当"一日生活流水"想成集。**轻提示对用户体验差**——能成集就直接成。

---

### #34 · T13_xiaowang_citywalk_uniform_low

| 字段 | 内容 |
|---|---|
| 路径 | L2 |
| 产品意图 | `show_mini_album`(低密度 citywalk 死穴,**靠主题独立成集**是关键测点) |
| 后端实际 | `suppress` · pattern `truth_table_final_F1` · album=[] |
| trigger 主题(6 张) | w35 弄堂晨光 / w36 咖啡馆雾窗 / w37 街头涂鸦 / w38 口袋公园 / w39 / w40 |

**为什么 mismatch**:同 #28——6 张 citywalk 多场景,后端 F1 兜底 suppress。这条**原本是设计来验证"低密度 DBSCAN 死穴靠主题救场"**的 → 后端目前没有"主题独立成集"这条救援路径,只看主题分散度就把它判没了。

---

### #41 · T22_li_camping_crossday_event

| 字段 | 内容 |
|---|---|
| 路径 | L2 |
| 产品意图 | `show_mini_album`(露营本来该成"露营集") |
| 后端实际 | `suppress` · pattern `truth_table_final_F1` · album=[] |
| trigger 主题(6 张) | l92 搭帐篷 / l93 篝火黄昏朋友 / l94 星空银河帐篷灯 / l95 日出晨雾金光 / l96 / l97 |

**为什么 mismatch**:6 张构成"露营→篝火→星空→日出"一个完整周末露营行程的不同时段,人眼一看是同一活动,**event_hint 都是 outing**。但每张具体主题词不同,**后端只看主题词分散度**没看 event 一致性 → F1 suppress。原 case 还压"跨业务日"那一面(我们没 captured_at)在 🟡 延后中,但即使没 time 维度,**主题分散度本不该这么严**。

---

### #44 · T7_li_around_landmark_small_loop

| 字段 | 内容 |
|---|---|
| 路径 | L2 |
| 产品意图 | `show_mini_album`(地标小环路,东方明珠 subject 撑成集) |
| 后端实际 | `show_inline_hint` · pattern `truth_table_final_E2` · album=[] |
| trigger 主题(6 张) | l65 东方明珠正面/北面 / l66 仰拍球体 / l67 黄浦江天际线倒影 / l68 日落剪影金光 / l69 / l70 |

**为什么 mismatch**:6 张都是上海东方明珠的不同角度,**salient_object 都是 oriental_tower**,产品意图就是"地标集"。但后端 E2 = "多个主载体弱+辅证中等" → 给个轻提示。**后端在 subject 维度(oriental_tower 重复)的权重不够**,没认出"同一地标多角度"应该强成集。这一条 Ace 5-28 已从 ⛔ 改 🟡,这次跑出来证实了"subject 撑主题成集"在后端是个 gap。

---

## B 类 · L2.5 维度判定不细(2 条)

### #47 · A4_zhang_granddaughter_grab

| 字段 | 内容 |
|---|---|
| 路径 | L2.5 |
| 产品意图 | `decision_tier ∈ [auto_merge, ask_user]`(孙女抓拍并入孙女相册) |
| 后端实际 | `decision_tier = [no_merge]` · pattern `G-F1` · album=[46] |
| trigger | z11 "孩子脸 近景 模糊"(单张) |
| setup seed(granddaughter_album) | z38 巧克力蛋糕 / z39 拆礼物 / z40 家宴 / + 3 张共 6 张 |

**为什么 mismatch**:setup 用孙女**生日庆祝**的 6 张建了 album=46,trigger z11 是"模糊的孙女脸抓拍"。两边**人物是同一个孙女**(people 维度强),但**主题词不重合**(蛋糕/礼盒/家宴 vs 孩子脸/近景/模糊)。后端 G-F1 = "不命中以上" → 后端**只看了主题相似度,没看 people 维度**。**真值表 G-B3 = "people=强 + 无 location 矛盾 → auto_merge" 本该命中**,但后端没启用 people 维度,直接 G-F1 兜底 no_merge。

---

### #59 · L25_zhang_event_conflict_to_album

| 字段 | 内容 |
|---|---|
| 路径 | L2.5 |
| 产品意图 | `decision_tier = ask_user`(event 冲突要让用户判) |
| 后端实际 | `decision_tier = [auto_merge]` · pattern `G-B2` · album=[53] |
| trigger | z44 "包间 气球装饰 纪念日晚餐" |
| setup seed(granddaughter_album) | z38 生日蛋糕 / z39 拆礼物 / z40 家宴 / + 3 张共 6 张 |

**为什么 mismatch**:setup 是孙女**生日庆祝**相册,trigger z44 是**老两口纪念日晚餐**(主体不同人、event 不同子类型)。**产品意图**:同属"家庭聚餐 event",但**纪念日 vs 生日是不同子事件**,人物也不同 → 应 ask_user 让用户选要不要合并。**后端实际**走 G-B2 = "event=强 + 无 location 矛盾 → auto_merge",**只识别到 event 大类相同(family_dinner),没识别到子类型冲突也没识别到 people 不同** → 直接 auto_merge 合了。这条是**后端冲突检测维度缺失**的典型。

---

## C 类 · 新码值待映射(1 条)

### #61 · L25_zhang_sensitive_to_any_no_merge

| 字段 | 内容 |
|---|---|
| 路径 | L2.5 |
| 产品意图 | `decision_tier ∈ [ask_user, no_merge]`(敏感照片不并任何相册) |
| 后端实际 | `decision_tier = [not_applicable]` · pattern `photo_sensitive_medium_or_high` · album=[] |
| trigger | z34 "候诊区 排队标识"(sensitive=medium) |
| setup seed(weekly_park_walk_album) | z07 花园清晨 / z09 玉兰花开 / z31 花园阳光长椅 |

**为什么 mismatch**:trigger 是 sensitive_level=medium 的医院候诊照,产品意图明确"敏感照片不参与 L2.5 合并"(红线 #6 / HR-PRE-02)→ 应 no_merge(或 ask_user 给用户选)。**后端实际**返回**新码值 `not_applicable`** + pattern `photo_sensitive_medium_or_high`,语义疑似 = "敏感照片直接绕过 L2.5 整条路径"。

**这条不是后端 bug,是台子 code_map 没桥接新码值**。产品行为对(敏感不并入),只是 not_applicable ≠ no_merge 字面上。**问后端同事确认**:`not_applicable` 在 sensitive 场景下是否等价 `no_merge`?如果是,加进 `reference/code_map.json` 的 `no_merge: ["no_merge", "not_applicable"]` 就 match 了。

---

## 给后端同事的话(8 条压成 4 个问题)

1. **L2 主路径主题分散度阈值偏严**(A 类 5 条):多元主题的"一次行程"被 F1 / D3 / E2 拒了。truth_table_final_F1 / D3 / E2 命中过早,产品意图允许"主题分散但同行程"成集。能不能松一档?
2. **L2.5 没启用 people 维度**(#47):G-B3 真值表本来定义了"people=强 → auto_merge",但 z11 孙女抓拍 vs 孙女生日相册,后端 G-F1 兜底没识别 people。后端 L2.5 是不是只看 theme/event/location 三维?
3. **L2.5 没识别 event 子类型冲突 + people 差异**(#59):纪念日 vs 生日,夫妻 vs 孙女,后端 G-B2 直接 auto_merge。后端 event 维度是不是只有大类没有子类型?后端是不是没看 people?
4. **新码 `not_applicable` 语义确认**(#61):sensitive 在 L2.5 返回这个新码,是否等价 `no_merge`?如果是,我们加 code_map 兼容,这条就 match。

---

配套:`api_harness/_DASHBOARD.html` 头部 filter 选 `mismatch` → 直接看这 8 条全部细节(每条都有输入照片标签 + 后端归因区折叠展开)。
